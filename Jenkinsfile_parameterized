#!groovy
//noinspection GroovyUnusedAssignment
@Library("Infrastructure@2.4.8") _

def product = "pip"
def component = "bootstrap"


withParameterizedInfraPipeline(product, "sbox", "sbox", component) {

}
